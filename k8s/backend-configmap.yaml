apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: petclinic
data:
  SPRING_PROFILES_ACTIVE: "postgres,spring-data-jpa"
  POSTGRES_URL: "jdbc:postgresql://postgres:5432/petclinic"
